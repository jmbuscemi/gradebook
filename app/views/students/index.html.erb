<h2> <%= session[:user_type].capitalize %> Page: Logged in as <%= @student.name %> </h2>
<p id="notice"><%= notice %></p>

<h3>Grade List</h3>

<table>
  <thead>
    <tr>
      <th>Assignment</th>
      <th>Grade</th>
    </tr>
  </thead>

  <tbody>
    <% @student.grades.each do |g| %>
      <tr>
        <td><%= g.assignment_name %></td>
        <td><%= g.grade %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
