<h2> You are logged in as <%= @student.name %> </h2>
<p id="notice"><%= notice %></p>

<h3>Grade List for <%= @student.name %></h3>

<table>
  <thead>
    <tr>
      <th>Assignment</th>
      <th>Grade</th>
    </tr>
  </thead>

  <tbody>
    <% @student.grades.each do |g| %>
      <tr>
        <td><%= g.assignment_name %></td>
        <td><%= g.grade %></td>
      </tr>
    <% end %>
  </tbody>
</table>
